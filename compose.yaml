services:
  go-app:
    container_name: go_app
    build:
      context: ./go
      dockerfile: Dockerfile
    platform: linux/amd64
    ports:
      - 8080:8080
    tty: true
    volumes:
      - .:/app:cached
    working_dir: /app
